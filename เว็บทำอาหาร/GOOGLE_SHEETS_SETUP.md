# 📊 คู่มือการตั้งค่า Google Sheets

## 🎯 วัตถุประสงค์
เชื่อมต่อระบบสั่งอาหารกับ Google Sheets เพื่อ:
- บันทึกออเดอร์อัตโนมัติ
- ดูประวัติการสั่งอาหารทั้งหมด
- วิเคราะห์ข้อมูลด้วย Google Sheets
- สำรองข้อมูลบน Cloud

---

## 📝 ขั้นตอนการตั้งค่า

### 1. สร้าง Google Sheets

1. เปิด [Google Sheets](https://sheets.google.com)
2. สร้าง Spreadsheet ใหม่
3. ตั้งชื่อ เช่น "ระบบสั่งอาหาร - ข้อมูลออเดอร์"
4. สร้าง Sheet 3 แผ่น:
   - **Pending** - ออเดอร์ที่รอยืนยัน
   - **Completed** - ออเดอร์ที่เสร็จแล้ว
   - **Menu** - รายการเมนูอาหาร

### 2. ตั้งค่า Header ใน Sheet "Pending"

ใส่ข้อมูลในแถวแรก:
```
PendingID | Timestamp | Username | Items_JSON | Total
```

### 3. ตั้งค่า Header ใน Sheet "Completed"

ใส่ข้อมูลในแถวแรก:
```
OrderID | Timestamp | Username | Items_JSON | Total | CompletedAt
```

### 4. ตั้งค่า Header ใน Sheet "Menu"

ใส่ข้อมูลในแถวแรก:
```
MenuID | Name | Price | ImageURL | Size
```

---

## 🔧 ตั้งค่า Google Apps Script

### 1. เปิด Script Editor

1. ใน Google Sheets ของคุณ
2. คลิก **Extensions** → **Apps Script**
3. ลบโค้ดเดิมทั้งหมด

### 2. วาง Code จากไฟล์ `apps_script.gs`

```javascript
// คัดลอกโค้ดทั้งหมดจากไฟล์ apps_script.gs
// แล้ววางในหน้า Apps Script Editor
```

### 3. Deploy as Web App

1. คลิก **Deploy** → **New deployment**
2. เลือก type: **Web app**
3. ตั้งค่า:
   - **Execute as:** Me
   - **Who has access:** Anyone
4. คลิก **Deploy**
5. **คัดลอก Web App URL** (จะใช้ในขั้นตอนถัดไป)

---

## 🔗 เชื่อมต่อกับระบบ

### 1. ตั้งค่าใน `admin.html`

เปิดไฟล์ `admin.html` แล้วค้นหา:

```javascript
const GOOGLE_SHEETS_URL = "https://docs.google.com/spreadsheets/d/YOUR_SPREADSHEET_ID/edit";
const WEB_APP_URL = "REPLACE_WITH_YOUR_WEB_APP_URL";
```

แก้ไขเป็น:

```javascript
const GOOGLE_SHEETS_URL = "https://docs.google.com/spreadsheets/d/1abc...xyz/edit"; // URL ของ Sheets ของคุณ
const WEB_APP_URL = "https://script.google.com/macros/s/ABC...XYZ/exec"; // Web App URL ที่คัดลอกมา
```

### 2. ตั้งค่าใน `staff.html`

เปิดไฟล์ `staff.html` แล้วค้นหา:

```javascript
const WEB_APP_URL = "REPLACE_WITH_YOUR_WEB_APP_URL";
```

แก้ไขเป็น:

```javascript
const WEB_APP_URL = "https://script.google.com/macros/s/ABC...XYZ/exec"; // Web App URL เดียวกัน
```

---

## 🧪 ทดสอบการทำงาน

### 1. ทดสอบส่งออเดอร์

1. เปิด `staff.html`
2. Login ด้วย staff1/staff1
3. เลือกเมนูอาหาร
4. กดยืนยันออเดอร์
5. ตรวจสอบใน Google Sheets → Sheet "Pending"

### 2. ทดสอบยืนยันออเดอร์

1. เปิด `admin.html`
2. ไปที่ "รายรับรายจ่าย"
3. กดปุ่ม "โหลดคำสั่งค้างจาก Google Sheet"
4. กดปุ่ม "ยืนยันและบันทึกลง Sheets"
5. ตรวจสอบใน Sheet "Completed"

### 3. ทดสอบปุ่มเปิด Google Sheets

1. เปิด `admin.html`
2. ไปที่ "จัดการผู้ใช้งาน"
3. กดปุ่ม "เปิด Google Sheets" (สีเขียว)
4. ควรเปิด Google Sheets ในแท็บใหม่

---

## 📍 ตำแหน่งปุ่ม Google Sheets

### ใน Admin:

1. **หน้าจัดการผู้ใช้งาน**
   - ปุ่มใหญ่สีเขียวด้านบน
   - ข้อความ: "เปิด Google Sheets"

2. **Modal ประวัติการสั่งอาหาร**
   - เมื่อกดปุ่ม "ดูประวัติ" ของ Staff
   - ปุ่มสีเขียวด้านล่างซ้าย
   - ข้อความ: "เปิด Google Sheets"

---

## 🔍 การแก้ปัญหา

### ปัญหา: กดปุ่มแล้วขึ้น Alert "กรุณาตั้งค่า Google Sheets URL"

**วิธีแก้:**
1. เปิดไฟล์ `admin.html`
2. ค้นหา `GOOGLE_SHEETS_URL`
3. แก้ไข URL ให้ถูกต้อง
4. บันทึกไฟล์
5. รีเฟรชหน้าเว็บ (Ctrl+Shift+R)

### ปัญหา: ส่งออเดอร์แล้วไม่ขึ้นใน Google Sheets

**วิธีแก้:**
1. ตรวจสอบว่า `WEB_APP_URL` ถูกต้องหรือไม่
2. ตรวจสอบว่า Deploy as Web App แล้วหรือยัง
3. ตรวจสอบว่าตั้งค่า "Who has access" เป็น "Anyone"
4. ลอง Deploy ใหม่อีกครั้ง

### ปัญหา: Permission Denied

**วิธีแก้:**
1. ใน Apps Script Editor
2. คลิก **Deploy** → **Manage deployments**
3. คลิก Edit (ไอคอนดินสอ)
4. เปลี่ยน "Who has access" เป็น "Anyone"
5. คลิก **Deploy**

---

## 📊 ตัวอย่างข้อมูลใน Google Sheets

### Sheet: Pending
```
PendingID          | Timestamp           | Username | Items_JSON                                    | Total
1699123456789      | 8/11/2568 09:46:31 | staff1   | [{"name":"ข้าวผัด","qty":2,"price":50}]     | 100
```

### Sheet: Completed
```
OrderID            | Timestamp           | Username | Items_JSON                                    | Total | CompletedAt
1699123456789      | 8/11/2568 09:46:31 | staff1   | [{"name":"ข้าวผัด","qty":2,"price":50}]     | 100   | 8/11/2568 10:00:00
```

---

## 🎨 ปรับแต่ง Google Sheets

### เพิ่มสูตรคำนวณ

**ยอดรวมทั้งหมด:**
```
=SUM(E2:E)
```

**จำนวนออเดอร์:**
```
=COUNTA(A2:A)
```

**ออเดอร์ของ Staff แต่ละคน:**
```
=COUNTIF(C2:C,"staff1")
```

### สร้างกราฟ

1. เลือกข้อมูลที่ต้องการ
2. คลิก **Insert** → **Chart**
3. เลือกประเภทกราฟ
4. ปรับแต่งตามต้องการ

---

## 🔐 ความปลอดภัย

### ข้อควรระวัง:

1. **อย่าแชร์ Web App URL** ในที่สาธารณะ
2. **ตั้งค่า Permission** ให้เหมาะสม
3. **สำรองข้อมูล** เป็นประจำ
4. **ตรวจสอบ Log** ใน Apps Script

### แนะนำ:

1. ใช้ Google Account ของบริษัท/องค์กร
2. ตั้งค่า 2-Factor Authentication
3. จำกัดการเข้าถึง Spreadsheet
4. ใช้ Version Control ใน Apps Script

---

## 📚 เอกสารเพิ่มเติม

- [Google Apps Script Documentation](https://developers.google.com/apps-script)
- [Google Sheets API](https://developers.google.com/sheets/api)
- [Web Apps Guide](https://developers.google.com/apps-script/guides/web)

---

## 💡 Tips

1. **ใช้ Filter View** ใน Google Sheets เพื่อกรองข้อมูล
2. **สร้าง Dashboard** ด้วย Google Data Studio
3. **ตั้งค่า Notification** เมื่อมีออเดอร์ใหม่
4. **Export ข้อมูล** เป็น CSV เพื่อวิเคราะห์เพิ่มเติม

---

## ✅ Checklist

- [ ] สร้าง Google Sheets แล้ว
- [ ] ตั้งค่า Header ครบทุก Sheet
- [ ] วาง Apps Script Code แล้ว
- [ ] Deploy as Web App แล้ว
- [ ] คัดลอก Web App URL แล้ว
- [ ] ตั้งค่าใน admin.html แล้ว
- [ ] ตั้งค่าใน staff.html แล้ว
- [ ] ทดสอบส่งออเดอร์แล้ว
- [ ] ทดสอบยืนยันออเดอร์แล้ว
- [ ] ทดสอบปุ่มเปิด Google Sheets แล้ว

---

🎉 **เสร็จสิ้น!** ตอนนี้ระบบของคุณเชื่อมต่อกับ Google Sheets แล้ว!
