# 🎯 การแก้ไขระบบ Online/Offline Status

## ✅ ปัญหาที่แก้ไข

### ปัญหาเดิม:
1. **เงื่อนไขการตรวจสอบไม่สอดคล้องกัน** - ใช้เวลา 1 นาที, 5 นาที ปะปนกัน
2. **ไม่มีการอัปเดตสถานะอัตโนมัติ** - Staff ไม่ได้อัปเดต lastActive
3. **ตรวจสอบเข้มงวดเกินไป** - ต้องผ่านทุกเงื่อนไข
4. **ไม่มี logging** - ยากต่อการ debug

### การแก้ไข:
1. ✅ **ใช้เกณฑ์เวลาเดียวกัน 2 นาที (120,000 ms)** สำหรับทุกการตรวจสอบ
2. ✅ **เพิ่มการอัปเดตสถานะอัตโนมัติ** ทุก 10 วินาที ทั้ง Admin และ Staff
3. ✅ **ปรับเงื่อนไขให้ยืดหยุ่น** - ผ่านเงื่อนไขใดเงื่อนไขหนึ่งก็ถือว่า online
4. ✅ **เพิ่ม console.log** เพื่อ debug ง่ายขึ้น

## 📋 รายละเอียดการแก้ไข

### 1. ไฟล์ `auth.js`
```javascript
// เพิ่มการบันทึก loginTime และ lastActive
// อัปเดตทั้ง last_login และ online_users พร้อมกัน
// เพิ่มฟังก์ชัน updateOnlineStatus()
```

**การเปลี่ยนแปลง:**
- เพิ่ม `loginTime` ใน online_users
- อัปเดต `last_login_${username}` ทุกครั้งที่ login
- เพิ่มฟังก์ชัน `updateOnlineStatus()` สำหรับอัปเดตสถานะ

### 2. ไฟล์ `admin.html`
```javascript
// ปรับปรุงฟังก์ชัน isUserOnline()
// เพิ่มการอัปเดตสถานะของ Admin เอง
// เพิ่ม logging สำหรับ debug
```

**การเปลี่ยนแปลง:**
- ใช้ `ONLINE_THRESHOLD = 120000` (2 นาที) แทนค่าต่างๆ
- ตรวจสอบ 3 แหล่ง: sessionStorage, online_users, last_login
- ถ้าผ่านเงื่อนไขใดเงื่อนไขหนึ่ง = Online
- เพิ่ม `updateAdminOnlineStatus()` ทำงานทุก 10 วินาที
- เพิ่ม console.log แสดงสถานะ

### 3. ไฟล์ `staff.html`
```javascript
// เพิ่มการอัปเดตสถานะอัตโนมัติ
// อัปเดตทั้ง last_login และ online_users
```

**การเปลี่ยนแปลง:**
- เพิ่ม `updateOnlineStatus()` ทำงานทุก 10 วินาที
- อัปเดตทั้ง `last_login_${username}` และ `online_users`
- เพิ่ม console.log แสดงการอัปเดต

### 4. Debug Panel ใน `admin.html`
```javascript
// รวม Debug Panel เข้าไปในหน้า Admin
// เปิด/ปิดได้ด้วยการคลิกการ์ด "Debug Status"
// Auto-refresh ทุก 3 วินาที เมื่อเปิด Panel
```

**การเปลี่ยนแปลง:**
- เพิ่มการ์ด "Debug Status" ในส่วนสถิติ (สีส้ม)
- เพิ่ม Debug Panel ที่ซ่อนไว้ เปิด/ปิดได้
- แสดงสถานะ Online/Offline แบบ real-time
- แสดงเวลาที่ผ่านมาเป็นวินาทีและนาที
- Auto-refresh ทุก 3 วินาที (เฉพาะเมื่อเปิด Panel)
- UI สีดำสวยงาม อ่านง่าย

## 🔍 วิธีการตรวจสอบสถานะ Online

ระบบจะตรวจสอบจาก **3 แหล่ง** ตามลำดับ:

### 1. sessionStorage (แท็บปัจจุบัน)
```javascript
const currentUser = sessionStorage.getItem('restaurant_current_user');
if (user.username === username) return true;
```
- ใช้สำหรับตรวจสอบ user ในแท็บปัจจุบัน
- ถ้าตรงกับ username ที่ต้องการ = Online

### 2. online_users (localStorage)
```javascript
const onlineUsers = JSON.parse(localStorage.getItem('online_users') || '[]');
const userOnline = onlineUsers.find(u => u.username === username);
if (userOnline && (now - userOnline.lastActive) < 120000) return true;
```
- ใช้สำหรับตรวจสอบ user จากแท็บอื่นในเบราว์เซอร์เดียวกัน
- ถ้า lastActive ภายใน 2 นาที = Online

### 3. last_login (localStorage)
```javascript
const lastLoginTime = localStorage.getItem(`last_login_${username}`);
if (lastLoginTime && (now - parseInt(lastLoginTime)) < 120000) return true;
```
- ใช้สำหรับตรวจสอบ user จากเบราว์เซอร์อื่น
- ถ้า login ภายใน 2 นาที = Online

## 🚀 วิธีทดสอบ

### 1. เปิดหน้า Debug ใน Admin
```
1. เปิด admin.html
2. ไปที่เมนู "จัดการผู้ใช้งาน"
3. คลิกที่การ์ด "Debug Status" (สีส้ม) ด้านบนขวา
4. Debug Panel จะเปิดขึ้นมา แสดงสถานะ real-time
```
- จะเห็นสถานะ real-time ของทุก user
- อัปเดตอัตโนมัติทุก 3 วินาที
- แสดงข้อมูลจาก 3 แหล่ง: sessionStorage, online_users, last_login

### 2. ทดสอบ Login
```
1. กด "Login" ในหน้า debug
2. เลือก user (admin, staff1, staff2)
3. ดูสถานะเปลี่ยนเป็น Online ทันที
```

### 3. ทดสอบ Multi-Tab
```
1. เปิด admin.html ในแท็บหนึ่ง
2. เปิด staff.html ในอีกแท็บ
3. Login ด้วย user ต่างกัน
4. ดูสถานะใน admin.html → จัดการผู้ใช้งาน
5. ทั้งสอง user ควรแสดง "🟢 Online"
6. เปิด Debug Panel เพื่อดูรายละเอียด
```

### 4. ทดสอบ Logout
```
1. Logout จาก staff.html
2. ดูสถานะใน admin.html
3. ควรเปลี่ยนเป็น "⚪ Offline" ภายใน 2 นาที
```

## 📊 Timeline การอัปเดต

```
Login
  ↓
[0s] บันทึก last_login + online_users
  ↓
[10s] อัปเดตสถานะครั้งที่ 1
  ↓
[20s] อัปเดตสถานะครั้งที่ 2
  ↓
[30s] อัปเดตสถานะครั้งที่ 3
  ↓
... (ทุก 10 วินาที)
  ↓
Logout
  ↓
[0s] ลบออกจาก online_users
  ↓
[120s] ถือว่า Offline (เกิน 2 นาที)
```

## 🎯 ผลลัพธ์ที่คาดหวัง

### ✅ กรณีที่ควรแสดง Online:
1. User login อยู่ในแท็บปัจจุบัน
2. User login อยู่ในแท็บอื่นของเบราว์เซอร์เดียวกัน
3. User login ภายใน 2 นาทีที่ผ่านมา

### ⚪ กรณีที่ควรแสดง Offline:
1. User ไม่ได้ login
2. User logout แล้ว
3. User login เกิน 2 นาทีแล้ว (ไม่มีการอัปเดตสถานะ)

## 🔧 การ Debug

### เปิด Console (F12)
```javascript
// ดูข้อมูล online_users
console.log(JSON.parse(localStorage.getItem('online_users')));

// ดูข้อมูล last_login
console.log(localStorage.getItem('last_login_admin'));
console.log(localStorage.getItem('last_login_staff1'));

// ดูข้อมูล current user
console.log(sessionStorage.getItem('restaurant_current_user'));
```

### Log Messages ที่ควรเห็น:
```
✅ Login success: admin - Added to online_users
🔄 Updated online status for admin
🔄 Admin status updated: admin
✅ admin is online (current tab)
✅ staff1 is online (online_users: 5s ago)
⚪ staff2 is offline
```

## 📝 หมายเหตุ

1. **เวลา 2 นาที** เป็นค่าที่เหมาะสมสำหรับระบบ
   - ไม่สั้นเกินไป (ไม่ทำให้ offline บ่อย)
   - ไม่ยาวเกินไป (แสดงสถานะได้แม่นยำ)

2. **การอัปเดตทุก 10 วินาที** ช่วยให้:
   - ระบบรู้ว่า user ยังใช้งานอยู่
   - ไม่ทำให้ระบบช้าเกินไป

3. **ใช้ sessionStorage สำหรับ current user**:
   - แยกข้อมูลแต่ละแท็บ
   - ไม่มีปัญหา cross-tab sync

## 🎉 สรุป

ระบบ Online/Offline Status ได้รับการปรับปรุงให้:
- ✅ ทำงานได้ทุกกรณี (same tab, other tabs, other browsers)
- ✅ แม่นยำและ real-time
- ✅ ง่ายต่อการ debug (รวมอยู่ใน Admin แล้ว)
- ✅ มี logging ที่ชัดเจน
- ✅ ใช้เกณฑ์เวลาที่สอดคล้องกัน (2 นาที)
- ✅ Debug Panel สะดวก เปิด/ปิดได้ง่าย

## 📍 วิธีใช้ Debug Panel

1. เปิด **admin.html**
2. ไปที่เมนู **"จัดการผู้ใช้งาน"**
3. คลิกการ์ด **"Debug Status"** (สีส้ม ด้านบนขวา)
4. Debug Panel จะเปิดขึ้นมา แสดง:
   - sessionStorage (แท็บปัจจุบัน)
   - online_users (แท็บอื่นในเบราว์เซอร์เดียวกัน)
   - last_login (ทุกเบราว์เซอร์)
5. คลิกปุ่ม **"ปิด"** เพื่อปิด Panel

**ทดสอบได้เลย!** 🚀
