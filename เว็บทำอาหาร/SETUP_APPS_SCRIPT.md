# 🚀 คู่มือการตั้งค่า Apps Script สำหรับ Online Status

## 📋 ข้อมูล Google Sheets ของคุณ

**Spreadsheet URL:**
```
https://docs.google.com/spreadsheets/d/184FGv623S8xLiMIRp3ksKJ1Qt-KmW8mrRXoF4pJ7y4c/edit?usp=sharing
```

**Spreadsheet ID:**
```
184FGv623S8xLiMIRp3ksKJ1Qt-KmW8mrRXoF4pJ7y4c
```

---

## 🔧 ขั้นตอนการตั้งค่า

### ขั้นตอนที่ 1: เปิด Apps Script Editor

1. เปิด Google Sheets ของคุณ:
   ```
   https://docs.google.com/spreadsheets/d/184FGv623S8xLiMIRp3ksKJ1Qt-KmW8mrRXoF4pJ7y4c/edit
   ```

2. คลิกที่เมนู **Extensions** → **Apps Script**

3. จะเปิดหน้าต่างใหม่ (Apps Script Editor)

---

### ขั้นตอนที่ 2: วางโค้ด Apps Script

1. ใน Apps Script Editor จะมีไฟล์ `Code.gs` อยู่แล้ว

2. **ลบโค้ดเดิมทั้งหมด** ในไฟล์ `Code.gs`

3. **คัดลอกโค้ดทั้งหมด** จากไฟล์ `apps_script.gs` ในโปรเจกต์นี้

4. **วางโค้ด** ลงในไฟล์ `Code.gs`

5. **บันทึก** (กด Ctrl+S หรือคลิกไอคอน 💾)

---

### ขั้นตอนที่ 3: Deploy เป็น Web App

1. คลิกปุ่ม **Deploy** (ด้านบนขวา) → **New deployment**

2. คลิกไอคอน **⚙️ (Settings)** ข้างๆ "Select type"

3. เลือก **Web app**

4. ตั้งค่าดังนี้:
   ```
   Description: Restaurant Online Status System
   Execute as: Me (your-email@gmail.com)
   Who has access: Anyone
   ```

5. คลิก **Deploy**

6. จะมี popup ขออนุญาต:
   - คลิก **Authorize access**
   - เลือกบัญชี Google ของคุณ
   - คลิก **Advanced** → **Go to [Project Name] (unsafe)**
   - คลิก **Allow**

7. คัดลอก **Web app URL** ที่ได้
   ```
   https://script.google.com/macros/s/XXXXXXXXXXXXX/exec
   ```

---

### ขั้นตอนที่ 4: ตั้งค่า URL ในโค้ด

#### 4.1 แก้ไขไฟล์ `admin.html`

เปิดไฟล์ `admin.html` และหาบรรทัดนี้ (ประมาณบรรทัดที่ 300-350):

```javascript
const WEB_APP_URL = "REPLACE_WITH_YOUR_WEB_APP_URL";
```

แก้ไขเป็น:

```javascript
const WEB_APP_URL = "https://script.google.com/macros/s/XXXXXXXXXXXXX/exec";
```

(แทนที่ `XXXXXXXXXXXXX` ด้วย URL ที่คุณคัดลอกไว้)

#### 4.2 แก้ไขไฟล์ `staff.html`

เปิดไฟล์ `staff.html` และหาบรรทัดนี้ (ประมาณบรรทัดที่ 50-100):

```javascript
const WEB_APP_URL = "REPLACE_WITH_YOUR_WEB_APP_URL";
```

แก้ไขเป็น URL เดียวกับใน `admin.html`

---

### ขั้นตอนที่ 5: ทดสอบระบบ

#### 5.1 ทดสอบ Web App

1. เปิด URL ที่ได้จาก Deploy ในเบราว์เซอร์:
   ```
   https://script.google.com/macros/s/XXXXXXXXXXXXX/exec
   ```

2. ควรเห็นข้อความ:
   ```json
   {"status":"ok","message":"Apps Script Web App is running"}
   ```

3. ถ้าเห็นข้อความนี้ = ✅ Web App ทำงานได้

#### 5.2 ทดสอบ Online Status

**ทดสอบในเบราว์เซอร์เดียวกัน:**

1. เปิด `admin.html` ในแท็บที่ 1
2. Login เป็น admin
3. เปิด `staff.html` ในแท็บที่ 2
4. Login เป็น staff1
5. กลับไปที่แท็บที่ 1 → ไปที่ "จัดการผู้ใช้งาน"
6. ควรเห็น staff1 แสดง "🟢 Online"

**ทดสอบข้ามเบราว์เซอร์:**

1. เปิด `admin.html` ใน **Chrome**
2. Login เป็น admin
3. เปิด `staff.html` ใน **Firefox** (หรือ Edge, Safari)
4. Login เป็น staff1
5. กลับไปที่ Chrome → คลิกการ์ด "Debug Status" (สีส้ม)
6. รอ 10-15 วินาที
7. ควรเห็น staff1 แสดง "🟢 Online" ใน section "4️⃣ Google Sheets"

---

### ขั้นตอนที่ 6: ตรวจสอบ Google Sheets

1. กลับไปที่ Google Sheets ของคุณ

2. ควรเห็น Sheet ใหม่ชื่อ **"OnlineUsers"** ถูกสร้างขึ้นอัตโนมัติ

3. ใน Sheet นี้จะมีข้อมูล:
   ```
   Username | Role  | LastActive    | Browser           | LoginTime
   admin    | admin | 1699999999999 | browser_abc123... | 1699999999999
   staff1   | staff | 1699999999999 | browser_xyz789... | 1699999999999
   ```

4. ถ้าเห็นข้อมูลนี้ = ✅ ระบบทำงานสมบูรณ์

---

## 🔍 การตรวจสอบและแก้ปัญหา

### ตรวจสอบว่า URL ถูกต้อง

เปิด Console (F12) ในเบราว์เซอร์และดู log:

```javascript
// ควรเห็น log เหล่านี้:
✅ Login success: admin - Added to online_users
🔄 Updated online status for admin
✅ Synced online status to Sheets: admin
✅ Fetched 2 online users from Sheets
```

### ถ้าเห็น Error

**Error: "Could not sync to Sheets"**
- ตรวจสอบว่าตั้งค่า WEB_APP_URL แล้ว
- ตรวจสอบว่า URL ถูกต้อง (ไม่มี REPLACE_WITH_YOUR_WEB_APP_URL)

**Error: "Failed to fetch"**
- ตรวจสอบว่า Deploy แล้ว
- ตรวจสอบว่าเลือก "Anyone" ใน Who has access
- ลอง Deploy ใหม่

**Error: "Authorization required"**
- ตรวจสอบว่า Authorize แล้ว
- ลอง Revoke และ Authorize ใหม่

---

## 📊 ตัวอย่างโค้ดที่ถูกต้อง

### admin.html (บรรทัดที่ 300-350)
```javascript
// ---------- Google Sheets Configuration ----------
const GOOGLE_SHEETS_URL = "https://docs.google.com/spreadsheets/d/184FGv623S8xLiMIRp3ksKJ1Qt-KmW8mrRXoF4pJ7y4c/edit";
const WEB_APP_URL = "https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec"; // <-- แก้ตรงนี้
```

### staff.html (บรรทัดที่ 50-100)
```javascript
const WEB_APP_URL = "https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec"; // <-- แก้ตรงนี้
```

---

## ✅ Checklist

- [ ] เปิด Google Sheets
- [ ] เปิด Apps Script Editor
- [ ] วางโค้ดจาก `apps_script.gs`
- [ ] บันทึกโค้ด
- [ ] Deploy เป็น Web app
- [ ] Authorize access
- [ ] คัดลอก Web App URL
- [ ] แก้ไข `admin.html` (WEB_APP_URL)
- [ ] แก้ไข `staff.html` (WEB_APP_URL)
- [ ] ทดสอบ Web App (เปิด URL ในเบราว์เซอร์)
- [ ] ทดสอบ Login ในเบราว์เซอร์เดียวกัน
- [ ] ทดสอบ Login ข้ามเบราว์เซอร์
- [ ] ตรวจสอบ Sheet "OnlineUsers" ถูกสร้าง
- [ ] ตรวจสอบข้อมูลใน Sheet

---

## 🎯 สรุป

หลังจากทำตามขั้นตอนทั้งหมด คุณจะได้:

1. ✅ ระบบ Online Status ที่ทำงานในเบราว์เซอร์เดียวกัน (ทันที)
2. ✅ ระบบ Online Status ที่ทำงานข้ามเบราว์เซอร์ (0-10 วินาที)
3. ✅ ข้อมูลถูกบันทึกใน Google Sheets
4. ✅ Debug Panel สำหรับตรวจสอบสถานะ

---

## 📞 ต้องการความช่วยเหลือ?

ถ้ามีปัญหาหรือข้อสงสัย:

1. เปิด Console (F12) และดู error messages
2. เปิด Debug Panel ใน admin.html
3. ตรวจสอบ Google Sheets ว่ามี Sheet "OnlineUsers" หรือไม่
4. ดูไฟล์ `CROSS_BROWSER_ONLINE_STATUS.md` สำหรับข้อมูลเพิ่มเติม

---

**หมายเหตุ:** ระบบนี้ใช้ Google Apps Script ซึ่งมี quota limit 20,000 requests/day สำหรับบัญชีฟรี ถ้ามี user เยอะอาจต้องเพิ่มเวลา interval หรือใช้ paid plan
