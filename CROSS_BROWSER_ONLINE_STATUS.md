# 🌐 คู่มือการตั้งค่าระบบ Online Status ข้ามเบราว์เซอร์

## 📋 ภาพรวม

ระบบนี้ใช้ **Google Sheets** เป็นตัวกลางในการเช็คสถานะ Online/Offline ข้ามเบราว์เซอร์

### ระดับการตรวจสอบ (4 ระดับ):

1. **sessionStorage** - แท็บปัจจุบัน (เร็วที่สุด)
2. **localStorage (online_users)** - แท็บอื่นในเบราว์เซอร์เดียวกัน (เร็ว)
3. **localStorage (last_login)** - เบราว์เซอร์เดียวกัน (เร็ว)
4. **Google Sheets** - ข้ามเบราว์เซอร์ (ช้ากว่า แต่ครอบคลุมทุกเบราว์เซอร์)

## 🚀 วิธีการตั้งค่า

### ขั้นตอนที่ 1: อัปเดต Google Apps Script

1. เปิด Google Sheets ของคุณ
2. ไปที่ **Extensions → Apps Script**
3. **แทนที่โค้ดทั้งหมด** ด้วยโค้ดใน `apps_script.gs`
4. **บันทึก** (Ctrl+S)
5. **Deploy → New deployment**
   - Select type: **Web app**
   - Execute as: **Me**
   - Who has access: **Anyone** (หรือ Anyone, even anonymous)
6. คัดลอก **Web App URL**

### ขั้นตอนที่ 2: ตั้งค่า WEB_APP_URL

เปิดไฟล์ `admin.html` และแก้ไขบรรทัดนี้:

```javascript
const WEB_APP_URL = "REPLACE_WITH_YOUR_WEB_APP_URL";
```

เปลี่ยนเป็น:

```javascript
const WEB_APP_URL = "https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec";
```

### ขั้นตอนที่ 3: ตั้งค่าใน staff.html (ถ้ามี)

เปิดไฟล์ `staff.html` และแก้ไขบรรทัดนี้:

```javascript
const WEB_APP_URL = "REPLACE_WITH_YOUR_WEB_APP_URL";
```

ใส่ URL เดียวกับใน admin.html

### ขั้นตอนที่ 4: ทดสอบระบบ

1. เปิด **admin.html** ใน Chrome
2. Login เป็น admin
3. เปิด **staff.html** ใน Firefox (หรือเบราว์เซอร์อื่น)
4. Login เป็น staff1
5. กลับมาที่ Chrome → เปิด Debug Panel
6. ควรเห็น staff1 แสดง "🟢 Online" ใน Google Sheets section

## 📊 Google Sheets Structure

ระบบจะสร้าง Sheet ชื่อ **"OnlineUsers"** โดยอัตโนมัติ:

| Username | Role  | LastActive    | Browser           | LoginTime     |
|----------|-------|---------------|-------------------|---------------|
| admin    | admin | 1699999999999 | browser_abc123... | 1699999999999 |
| staff1   | staff | 1699999999999 | browser_xyz789... | 1699999999999 |

### คอลัมน์:
- **Username**: ชื่อผู้ใช้
- **Role**: สิทธิ์ (admin/staff)
- **LastActive**: เวลา active ล่าสุด (timestamp)
- **Browser**: Browser ID (unique ต่อเบราว์เซอร์)
- **LoginTime**: เวลา login (timestamp)

## 🔄 การทำงานของระบบ

### เมื่อ Login:
1. บันทึกลง sessionStorage (แท็บปัจจุบัน)
2. บันทึกลง localStorage (online_users, last_login)
3. ส่งข้อมูลไปยัง Google Sheets (ถ้าตั้งค่าแล้ว)

### ทุก 10 วินาที:
1. อัปเดต localStorage (online_users, last_login)
2. ส่งข้อมูลไปยัง Google Sheets

### เมื่อ Logout:
1. ลบออกจาก sessionStorage
2. ลบออกจาก localStorage (online_users)
3. ลบออกจาก Google Sheets

### เมื่อตรวจสอบสถานะ:
1. เช็ค sessionStorage (แท็บปัจจุบัน)
2. เช็ค localStorage (แท็บอื่นในเบราว์เซอร์เดียวกัน)
3. เช็ค last_login (เบราว์เซอร์เดียวกัน)
4. เช็ค Google Sheets (ข้ามเบราว์เซอร์)

## 🎯 ตัวอย่างการใช้งาน

### กรณีที่ 1: เบราว์เซอร์เดียวกัน
```
Chrome (แท็บ 1): admin.html - admin login
Chrome (แท็บ 2): staff.html - staff1 login

ผลลัพธ์: ทั้งสองเห็นกันเป็น Online ทันที (ใช้ localStorage)
```

### กรณีที่ 2: ข้ามเบราว์เซอร์
```
Chrome: admin.html - admin login
Firefox: staff.html - staff1 login

ผลลัพธ์: 
- Chrome เห็น staff1 เป็น Online ภายใน 10 วินาที (ใช้ Google Sheets)
- Firefox เห็น admin เป็น Online ภายใน 10 วินาที (ใช้ Google Sheets)
```

### กรณีที่ 3: ข้ามคอมพิวเตอร์
```
คอมพิวเตอร์ A: admin.html - admin login
คอมพิวเตอร์ B: staff.html - staff1 login

ผลลัพธ์: ทั้งสองเห็นกันเป็น Online ภายใน 10 วินาที (ใช้ Google Sheets)
```

## 🔍 การ Debug

### เปิด Debug Panel:
1. เปิด admin.html
2. ไปที่ "จัดการผู้ใช้งาน"
3. คลิกการ์ด "Debug Status" (สีส้ม)
4. ดูข้อมูลจาก 4 แหล่ง

### ตรวจสอบ Console:
```javascript
// ดูข้อมูล Sheets
console.log(window.sheetsOnlineUsers);

// ดูข้อมูล localStorage
console.log(JSON.parse(localStorage.getItem('online_users')));

// ดู Browser ID
console.log(localStorage.getItem('browser_id'));
```

### ตรวจสอบ Google Sheets:
1. เปิด Google Sheets
2. ดู Sheet "OnlineUsers"
3. ควรเห็น user ที่ login อยู่

## ⚠️ ข้อควรระวัง

### 1. ความล่าช้า
- localStorage: อัปเดตทันที
- Google Sheets: อัปเดตทุก 10 วินาที
- อาจมีความล่าช้า 0-10 วินาที

### 2. Quota ของ Google Apps Script
- ฟรี: 20,000 requests/day
- ถ้ามี user เยอะ อาจเกิน quota
- แนะนำ: เพิ่มเวลา interval หรือใช้ paid plan

### 3. Browser ID
- แต่ละเบราว์เซอร์จะมี ID ไม่ซ้ำกัน
- ถ้าล้าง localStorage จะได้ ID ใหม่
- อาจมี user ซ้ำใน Sheets (แต่ไม่กระทบการทำงาน)

## 🛠️ การแก้ปัญหา

### ปัญหา: ไม่เห็นสถานะข้ามเบราว์เซอร์
**วิธีแก้:**
1. ตรวจสอบว่าตั้งค่า WEB_APP_URL แล้ว
2. เปิด Console ดู error
3. ตรวจสอบว่า Deploy Apps Script แล้ว
4. ตรวจสอบว่าเลือก "Anyone" ใน Who has access

### ปัญหา: Sheets ไม่อัปเดต
**วิธีแก้:**
1. ตรวจสอบ Console ว่ามี error
2. ตรวจสอบว่า URL ถูกต้อง
3. ลอง Deploy ใหม่
4. ตรวจสอบ Quota ของ Apps Script

### ปัญหา: แสดง Online แต่จริงๆ Offline
**วิธีแก้:**
1. รอ 2 นาที (ONLINE_THRESHOLD)
2. ตรวจสอบว่า user logout หรือไม่
3. ตรวจสอบ timestamp ใน Sheets

## 📝 สรุป

ระบบนี้ใช้ **4 ระดับ** ในการตรวจสอบสถานะ:

1. ✅ **sessionStorage** - แท็บปัจจุบัน (ทันที)
2. ✅ **localStorage** - เบราว์เซอร์เดียวกัน (ทันที)
3. ✅ **last_login** - เบราว์เซอร์เดียวกัน (ทันที)
4. ✅ **Google Sheets** - ข้ามเบราว์เซอร์ (0-10 วินาที)

**ข้อดี:**
- ✅ ทำงานได้ทุกกรณี (same tab, same browser, cross browser, cross computer)
- ✅ เร็วสำหรับเบราว์เซอร์เดียวกัน
- ✅ ครอบคลุมข้ามเบราว์เซอร์
- ✅ ไม่ต้องใช้ Server

**ข้อจำกัด:**
- ⚠️ ต้องตั้งค่า Google Sheets
- ⚠️ มีความล่าช้า 0-10 วินาที สำหรับข้ามเบราว์เซอร์
- ⚠️ มี Quota limit (20,000 requests/day)

**เหมาะสำหรับ:**
- ร้านอาหารขนาดเล็ก-กลาง
- ทีมงาน 5-20 คน
- ไม่ต้องการ real-time แบบ instant (ยอมรับความล่าช้า 10 วินาที)
